import { defineNuxtPlugin } from "#app";
import VueList from "@7span/vue-list/src/main";

export default defineNuxtPlugin((nuxtApp) => {
  // const options = useRuntimeConfig().vueList;
  // const app = nuxtApp.vueApp;
  // app.use(VueList, options);
});
